---
name: key_board
description: 通用 skill 生命周期管理器（总结 / 反思 / 更新 / 同步）。主文档只做路由，按需加载 references/ 里的具体动作 SOP。触发包括"总结成 skill"、"保存对话为 skill"、"沉淀为 reference"、"skill 膨胀了 / 拆出去"、"合并 skill"、"扫现有 skill"等。路由优先级：先按信号匹配现有 ref（沉淀为ref / 结构优化 / 合并审计 / 合并执行）；无相关主题才加载 [[创建新skill]] 走从 0 创建流程。
---
# Key Board — Skill 生命周期路由器

## 触发条件

| 用户说 | 触发动作 | 加载 ref |
| --- | --- | --- |
| "把 xx 沉淀为 reference" / "拆出去" / "skill 太大了 / 膨胀了" | 拆 ref（单主题或批量） | [[沉淀为ref]] |
| "给 skill 加 ref 引导" / "统一所有 skill 的 ref 架构" | 补引导 / 统一架构 | [[结构优化]] |
| "探索 / 扫描现有 skill" / "哪些 skill 可合并" | 横向扫描 → 只出报告 | [[合并审计]] |
| "合并 skill" / "把多个 skill 合成一个" | 4 阶段动手合并（确需合并后） | [[合并执行]] |
| "总结成 skill" / "做成 skill" / "保存对话为 skill" / **无现有 ref 可扩展时** | 从 0 创建新 skill | [[创建新skill]] |

## 路由优先级（最重要）

**先扩展现有 ref，无相关主题才创建新 skill。**

处理一个用户信号时按顺序判断：

1. 信号是否匹配 [[沉淀为ref]] / [[结构优化]] / [[合并审计]] / [[合并执行] 任一？
   - **是** → 加载对应 ref 走 SOP；不要触发创建
   - **否** → 进入第 2 步
2. 是否需要新增 ref 来承载这个场景？
   - **是** → 走 [[沉淀为ref]] 的「批量模式」或扩展现有 ref 树
   - **否**（这是全新的、独立的主题） → 加载 [[创建新skill]]

> **为什么有这个优先级**：通用总结反思更新同步 skill 的本职是**让现有 skill 体系持续演化**，不是无脑制造新 skill。新增 ref 是「体系内演化」（低成本），创建新 skill 是「体系外扩张」（高成本）。先内后外。

## 平面扩展原则

**主 skill 只负责路由，不负责具体处理场景。**

- 主文档只含：触发条件表 + 路由优先级 + 路由表 + 主流程骨架。任何场景的执行细节一律在 references/
- **扩展模式固定**：新场景 → 新建 ref 文件 + 路由表加一行，主文档不加章节、不长胖
- 主文档行数不随场景数增长；ref 数量可以无限扩展

## 核心原则

**主 SKILL.md 负责原则和 ref-map；references/ 才存放具体场景。**

| 文档 | 角色 | 只放 | 不放 |
| --- | --- | --- | --- |
| 主 SKILL.md | 原则 + ref-map | 触发条件、路由优先级、主流程骨架、检查清单、路由表 | 任何场景级细节 |
| references/<ref>.md | 具体动作 SOP | 某动作的完整 SOP、示例、正反例、判定细节 | 通用原则（那是主文档的事） |

**演化反模式**：v1 把场景内容全写在主文档是合理的（还没有 ref）；一旦某主题拆出 ref，主文档旧内容必须**同步清空**，原位置只留一句原则 + `[[锚点]]`。否则主文档兼具"场景承载 + ref-map"双角色，行文混乱。主文档随 ref 增多而**变薄**，不是变厚。

### 边界

| 不做什么 | 做什么 |
| --- | --- |
| ❌ 不无脑创建新 skill | ✅ 路由优先级保证先扩展 ref |
| ❌ 不改其他 skill 的核心流程/触发条件（除非用户授权） | ✅ 按主题把 skill 组织成 主文档 + 特化 ref |
| ❌ 不动 skill 的代码实现 | ✅ 只搬文档结构 |

## 场景路由（ref-map）

| 场景 | 信号 | 何时读取 | 路径 |
| --- | --- | --- | --- |
| ★ 沉淀为 ref（含膨胀拆分批量模式、第二层切分、操作记录模板） | "沉淀为 reference / 拆出去 / skill 膨胀了" | 触发拆 ref 类动作时 | references/沉淀为ref.md |
| 结构优化（补引导 + 统一架构） | "加 ref 引导 / 统一 ref 架构" | 触发 B/D 场景时 | references/结构优化.md |
| 合并审计（探索模式） | "扫一遍 / 哪些可合并" | 触发探索类诉求时（只出报告不动手） | references/合并审计.md |
| 合并执行 | "合并 skill" | 主流程判断**确需合并后** | references/合并执行.md |
| 创建新 skill（从 0 到 1） | "总结成 skill" / 无现有 ref 可扩展时 | 路由优先级判定后才加载 | references/创建新skill.md |

> **合并场景注意**：先走主流程判断（是否真要合并？拆出去不行吗？），确认后才加载 [[合并执行]]。未指定目标的探索式诉求先走 [[合并审计]]。
>
> **创建场景注意**：路由优先级永远先尝试扩展现有 ref，无相关主题才走 [[创建新skill]]。

## 主流程骨架（5 步）

1. **识别作用域**：用户信号是什么？目标对象是哪个 skill（创建新 skill 时无对象）？有无不可动章节？
2. **路由判定**：按触发条件表查 → 是否匹配现有 ref？走第 3 步（执行）或第 4 步（先评估优先级）
3. **设计方案**：加载对应 ref，按其设计要点出方案（ref 名称、路径、加载时机、为何是独立主题）
4. **执行**：按 ref 的 SOP 执行。共同骨架 = 完整迁移内容 → 原位置留 `[[锚点]]` + 一句总结 → 路由表登记。**下沉必须彻底**
5. **校验**：对照 ref 内的检查清单逐项过

## 易错和坑（高频 5 条）

| 错误 | 根因 | 预防 |
| --- | --- | --- |
| 看到「总结成 skill」就无脑走创建 ref | 没看路由优先级 | 必须先查 4 个现有 ref 是否可扩展 |
| 顺手改了目标 skill 的核心流程/触发条件 | 越权润色 | 本 skill 只做结构，核心流程禁止动 |
| 把元层依赖（skill-creator / writing-skills）写进新 skill | 两层依赖合并 | 见 [[创建新skill]] 的依赖声明规范 |
| 把主流程骨架拆进 ref | 误以为所有内容都可沉淀 | 主流程、触发条件、检查清单必须留在主文档 |
| 拆完忘记登记路由表 | ref 成为孤儿文档 | 每建一个 ref 立即在路由表登记"何时读取" |

## 错误案例记录

每次执行后，在本次操作的 skill 末尾追加操作教训（如有踩坑），模板见 [[沉淀为ref]] 的「操作记录模板」。

## 成功标准检查清单

- [ ] 路由优先级被遵守（先扩展 ref，无相关主题才创建）
- [ ] 主 SKILL.md 只含原则与路由，无场景级内容残留
- [ ] 主题在主文档和 ref 之间分配清晰、不重复
- [ ] 所有 ref 在路由表登记，且写明"何时读取"
- [ ] `[[ref-name]]` 链接与文件名一一对应
- [ ] 目标 skill 的核心流程/触发条件未变（除非用户授权）
- [ ] 没有空 ref 文档
- [ ] 用户确认了拆分方案（如未明确授权）

## 何时**不**触发本 skill

- 用户想**改 skill 的代码/实现** → 本 skill 不管代码
- 用户想**优化触发描述 / 评估 skill 效果** → `skill-creator` 的 description 优化 / eval 流程
- 目标 skill 无独立主题且不需创建 → 不要硬拆，劝退

## 与其他 skill 的协作

```
key_board        →  skill 生命周期管理（路由 + 5 ref SOP）
skill-creator    →  评估/优化/打包 skill (质量保障)
```

本 skill 是 skill 体系的"路由器 + 演化执行器"；`skill-creator` 是"质量评估与优化器"。两者职责不同，不重叠。
