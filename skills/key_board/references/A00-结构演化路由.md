---
name: A00-结构演化路由
description: A 序列（结构演化）的元数据与序列内路由细则。进入 A 序列任一动作前先读本文件：用于区分 A01 沉淀为ref / A02 结构优化 / A03 ref路由优化 的边界与升级路径。
---

# A00 — A 序列元数据：结构演化路由

> **本文件不承载任何动作 SOP**，只回答一个问题：**这个信号该进 A 序列的哪个成员？** 成员的执行细节一律看对应 ref。

## 序列画像

| 项 | 值 |
| --- | --- |
| 序列代号 | A |
| 类别 | 结构演化——不动核心流程，只动文档结构 |
| 成员 | A01 沉淀为ref / A02 结构优化 / A03 ref路由优化 |
| 共同红线 | 不改目标 skill 的核心流程/触发条件；主文档只留原则 + 锚点 |

## 序列内路由（信号 → 成员）

| 用户信号 | 成员 | 边界提示 |
| --- | --- | --- |
| "把 xx 拆出去 / 沉淀为 reference / skill 膨胀了" | [[A01-沉淀为ref]] | 从主文档抽主题 → 新 ref，ref 数 +1 |
| "给 ref 加加载引导 / 统一 ref 架构" | [[A02-结构优化]] | 只加引导/统一格式，ref 数不变 |
| "在现有 ref 里加一节" | [[A02-结构优化]]（场景 B 扩展） | ⚠️ 不是拆 ref——ref 数不变，别走 A01 |
| "ref 太多了 / 编号 / 按序列归类"（ref > 5） | [[A03-ref路由优化]] | 只重命名 + 归类 + 建代号，不迁移内容 |
| 创建全新 skill | 不属于本序列 → [[C01-创建新skill]] | 由主文档路由优先级兜底 |

## 序列内升级路径

- A02 统一架构时发现场景内容混在主文档 → **顺手下沉**（走 [[A01-沉淀为ref]] 的 Step 1-5）
- A01 / A02 执行后目标 skill 的 ref 总数跨过 5 → 触发 [[A03-ref路由优化]] 补序列代号
- 任一成员执行完 → 回主文档路由表登记"何时读取"；发现本表未覆盖的边界信号 → 把新行补进本文件（元数据 ref 的职责就是持续收敛序列内路由）
